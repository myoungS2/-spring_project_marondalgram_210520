<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.marondalgram.post.dao.PostDAO">
	<select id="selectPostListByUserId" parameterType="int" resultType="com.marondalgram.post.model.Post">
			SELECT
				`id`
				, `userId`
				, `imgUrl`
				, `content`
				, `createdAt`
				, `updatedAt`
			FROM
				`post`
			WHERE
				`userId` = #{userId}	
	</select>
	
	<insert id="insertPost" parameterType="map">
		INSERT INTO `post`
		(
			`userId`
			, `content`
			, `imgUrl`
			, `createdAt`
			, `updatedAt`
		)
		VALUES
		(
			#{userId}
			, #{content}
			, #{imgUrl}
			, NOW()
			, NOW()
		)	
	</insert>
</mapper>